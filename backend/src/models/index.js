const User = require('./User.model');
const Video = require('./video.model');
const Tenant = require('./Tenant.model');
const ProcessingJob = require('./ProcessingJob.model');
const Invitation = require('./Invitation.model');

module.exports = {
  User,
  Video,
  Tenant,
  ProcessingJob,
  Invitation
};
